extends layout

block content
  h1= title

  ul
    each val in weaponinstance_list
      li
        a(href=val.url) #{val.weapon.name} : #{val.nickname}

    else
      li There are no weapon copies in this library.